# Mission : Agent IA & Interface Web de Récapitulatif d'Emails

Ce dépôt contient le code source et la documentation pour la mission de développement d'une interface web couplée à une automatisation n8n.

## 🏗 Architecture Choisie
* **Frontend :** React (via Vite) pour une interface dynamique et responsive.
* **Automatisation :** n8n (Self-hosted) pour la récupération, le traitement et le résumé des emails via IA (Groq).
* **Données :** Stockage local via fichier JSON (`mails-today.json`).

## 🚀 Étapes d'installation

### Pré-requis
* Node.js installé.
* n8n installé en local.

### Installation de l'Interface Web
1.  Cloner le dépôt.
2.  Accéder au dossier `web-interface`.
3.  Lancer `npm install`.
4.  Lancer `npm run dev` pour démarrer le serveur local.

## 📝 Résultats Obtenus
*(à compléter avec des captures d'écran)*

## ⚠️ Difficultés Rencontrées
*(À compléter durant le développement)*

---
*Projet réalisé dans le cadre de l'exercice Agent IA Web.*